fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
zeroconf>=0.132,<0.133

# A.I. Duet (Performance RNN) — CoreML conversion + validation tooling
#
# Used by:
# - `python_backend/duet/convert_performance_rnn_to_coreml.py`
# - `python_backend/duet/validate.py`
#
# Notes:
# - Keep `numpy` pinned to a NumPy 1.x release compatible with `tensorflow-macos==2.9.1`.
tensorflow-macos==2.9.1
coremltools==7.2
note-seq==0.0.3
numpy==1.23.5
protobuf==3.19.6

# Optional Magenta pins: see `requirements-magenta.txt`
